<?php
/**
 * WorkPages - Konfiguration
 *
 * Kopieren Sie diese Datei nach config.php und passen Sie die Werte an.
 * Alternativ wird config.php automatisch vom Installer erstellt.
 */

return [
    // Datenbank
    'DB_HOST'   => 'localhost',
    'DB_NAME'   => 'workpages',
    'DB_USER'   => 'root',
    'DB_PASS'   => '',
    'DB_CHARSET'=> 'utf8mb4',

    // Applikation
    'BASE_URL'  => 'http://localhost/public',
    'APP_KEY'   => 'CHANGE-ME-to-a-random-string-of-at-least-32-chars',
    'APP_NAME'  => 'WorkPages',
    'APP_ENV'   => 'production', // 'development' fuer Entwicklung

    // Debug: wenn true und User ist Admin, werden Details bei Fehlern angezeigt
    'DEBUG'     => false,

    // Suche: 'like' (immer), 'fulltext' (FULLTEXT Indexes noetig), 'auto'
    'SEARCH_MODE' => 'like',

    // Activity Log: 'json' (native JSON-Spalte) oder 'text' (JSON als TEXT)
    'ACTIVITY_META_MODE' => 'text',

    // Installer: auf true setzen um den Installer erneut ausfuehren zu koennen
    'INSTALL_UNLOCK' => false,

    // Pfade (normalerweise keine Aenderung noetig)
    'LOG_FILE'  => __DIR__ . '/../storage/logs/app.log',
    'UPLOAD_DIR'=> __DIR__ . '/../storage/uploads',
];
